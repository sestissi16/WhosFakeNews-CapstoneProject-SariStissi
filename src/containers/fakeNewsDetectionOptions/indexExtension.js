import React from 'react'

const Extension = () => (
  <div>
    <h1>Extension Page</h1>
    <p>
        This is where you find out what the extension does, see more detailed information from the extension, 
        and find instructions on how to install it.
    </p>
  </div>
)

export default Extension